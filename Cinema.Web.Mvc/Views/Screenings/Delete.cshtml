@model Cinema.Dto.ViewModels.Screenings.ScreeningIndexVM

@{
    ViewData["Title"] = "Delete";
}
    <div class="section section-md" asp-authorize asp-roles="@Roles.Administrator">
        <div class="container">
            <div class="row">
                <div class="col-12">
                    <h1>Delete</h1>

                    <h3>Are you sure you want to delete this screening?</h3>
                    <div>
                        <h4>Screening</h4>
                        <hr />
                        <dl class="row">
                            <dt class="col-sm-2">
                                @Html.DisplayNameFor(model => model.DateAndTime)
                            </dt>
                            <dd class="col-sm-10">
                                @Html.DisplayFor(model => model.DateAndTime)
                            </dd>
                        </dl>

                        <form method="post">
                            <input type="hidden" asp-for="Id" />
                            <input type="submit" value="Delete" class="btn btn-danger" /> |
                            <a asp-action="Index">Back to List</a>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>